import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#fbe9e7', // Um laranja bem claro
  },
  text: {
    fontSize: 24,
    fontWeight: 'bold',
  },
});

export default styles;